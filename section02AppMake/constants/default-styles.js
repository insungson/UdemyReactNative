// 아래처럼 default style을 적용시킬 css 부분을 만들어도 된다!!
import { StyleSheet } from "react-native";

export default StyleSheet.create({
  bodyText: {
    fontFamily: "open-sans",
  },
  title: {
    fontFamily: "open-sans-bold",
    fontSize: 18,
  },
});
